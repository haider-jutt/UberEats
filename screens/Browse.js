import { ScrollView, StyleSheet, Text, View } from "react-native";
import { useEffect } from "react";

const Browse = () => {
  return (
   <View>
    <Text>
    Welcome to Browse Screen
    </Text>
    </View>
  );
};

export default Browse;

const styles = StyleSheet.create({});
